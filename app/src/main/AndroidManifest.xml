<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.akvelon.myfirstapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppDelegate"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".lesson1.CounterActivity" />

        <activity android:name=".lesson2.intents.ColorChooserActivity" />
        <activity android:name=".lesson2.intents.ColorDetailsInfoActivity" />
        <activity android:name=".lesson2.recyclerview.RecyclerViewActivity" />

        <activity android:name=".lesson3.ui.DayForecastActivity" />

        <activity android:name=".lesson4.rxweather.ui.RxDayForecastActivity" />

        <activity android:name=".lesson5.fragments.static_fragments.StaticFragmentsHostActivity" />
        <activity android:name=".lesson5.fragments.dynamic_fragments.DynamicFragmentsHostActivity" />

        <activity android:name=".lesson6.sqlite.SqliteActivity" />

    </application>

</manifest>